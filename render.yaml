services:
  - type: web
    name: valentines-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        scope: build
        sync: false
      - key: NODE_ENV
        value: production
